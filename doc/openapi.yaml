openapi: 3.0.0
info:
  version: 1.0.0
  title: Python Service for Cloud Run API
  description: This is a sample OpenAPI spec for the template Python Service gor Google Cloud Run
servers:
  - url: http://localhost:8080
    description: Local testing server
paths:
  /:
    get:
      description: Returns 'ok' if everything is looking good
      responses:
        '200':
          description: Successful response
